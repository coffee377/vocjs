{
  "name": "vocjs",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "build:watch": "lerna run build:watch",
    "clean": "lerna clean -y"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@umijs/fabric": "^2.1.0",
    "cross-env": "^7.0.2",
    "prettier": "^2.0.5",
    "shelljs": "^0.8.4"
  }
}
