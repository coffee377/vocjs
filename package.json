{
  "name": "vocjs",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "build:watch": "lerna run build:watch",
    "clean": "lerna clean -y"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "cross-env": "^6.0.3",
    "shelljs": "^0.8.3",
    "@umijs/fabric": "^2.0.0",
    "prettier": "^1.19.1"
  }
}
