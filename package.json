{
  "name": "vocjs",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "build:watch": "lerna run build:watch",
    "clean": "lerna clean -y"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "cross-env": "^7.0.2",
    "shelljs": "^0.8.4",
    "@umijs/fabric": "^2.1.0",
    "prettier": "^2.0.5"
  }
}
